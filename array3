#include <iostream>
using namespace std;

void printArray(int arr[], int size) {
    for(int x=0; x<size; x++) {
        cout <<arr[x]<< endl;
    }
}

int main() {
    int numbers[]= {42, 33, 88, 4, 244};
    int size = sizeof(numbers) / sizeof(numbers[0]);
    printArray(numbers, size);
}
